@page
@model Website.Pages.MenuModel
@{
    ViewData["Title"] = "Menu";
}

<h1>Menu</h1>

<div id="available-items">
    <h2>Combos</h2>
    <div class="menu-item">
        <h3>Combo Name</h3>
        <h3>Price</h3>
        <h3>Calories</h3>
        <h3>Ingredients</h3>
    </div>
    @foreach (var combo in @Model.menu.AvailableCombos)
    {
        <div class="menu-item">
            <div class="item-name">@combo.Description</div>
            <div class="item-price">$@combo.Price.ToString("0.00")</div>
            <div class="item-calories">@combo.Calories</div>
            <div class="item-ingredients">
                @foreach (var ingredient in combo.Ingredients)
                {
                    <div class="item-ingredient">
                        @ingredient
                    </div>
                }
            </div>
        </div>
    }

    <br />

    <h2>Entrees</h2>
    <div class="menu-item">
        <h3>Entree Name</h3>
        <h3>Price</h3>
        <h3>Calories</h3>
        <h3>Ingredients</h3>
    </div>
    @foreach (var entree in @Model.menu.AvailableEntrees)
    {
        <div class="menu-item">
            <div class="item-name">@entree.Description</div>
            <div class="item-price">$@entree.Price.ToString("0.00")</div>
            <div class="item-calories">@entree.Calories</div>
            <div class="item-ingredients">
                @foreach (var ingredient in entree.Ingredients)
                {
                    <div class="item-ingredient">
                        @ingredient
                    </div>
                }
            </div>
        </div>
    }

    <br />

    <h2>Sides</h2>
    <div class="menu-item">
        <h3>Side Name</h3>
        <h3>Price</h3>
        <h3>Calories</h3>
        <h3>Ingredients</h3>
    </div>
    @foreach (var side in @Model.menu.AvailableSides)
    {
        <div class="menu-item">
            <div class="item-name">@side.Description</div>
            <div class="item-price">$@side.Price.ToString("0.00")</div>
            <div class="item-calories">@side.Calories</div>
            <div class="item-ingredients">
                @foreach (var ingredient in side.Ingredients)
                {
                    <div class="item-ingredient">
                        @ingredient
                    </div>
                }
            </div>
        </div>
    }

    <br />

    <h2>Drinks</h2>
    <div class="menu-item">
        <h3>Drink Name</h3>
        <h3>Price</h3>
        <h3>Calories</h3>
        <h3>Ingredients</h3>
    </div>
    @foreach (var drink in @Model.menu.AvailableDrinks)
    {
        <div class="menu-item">
            <div class="item-name">@drink.Description</div>
            <div class="item-price">$@drink.Price.ToString("0.00")</div>
            <div class="item-calories">@drink.Calories</div>
            <div class="item-ingredients">
                @foreach (var ingredient in drink.Ingredients)
                {
                    <div class="item-ingredient">
                        @ingredient
                    </div>
                }
            </div>
        </div>
    }
</div>
